const magic = () => new Date();

// any arg
const myConcat = (arr1, arr2) => {
    return arr1.concat(arr2);
};

//set default parametr 
// Only change code below this line
const increment = (number = 0, value = 1) => number + value;
// Only change code above this line
